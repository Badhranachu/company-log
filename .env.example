# Leave VITE_API_URL and VITE_WS_URL empty in production.
# Nginx proxies /api/ and /ws/ to the backend automatically,
# so the frontend uses relative paths and works for ALL devices.
VITE_API_URL=
VITE_WS_URL=
